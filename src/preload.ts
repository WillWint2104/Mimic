// Preload script. Currently empty — no IPC bridges yet.
// When the renderer needs access to privileged APIs (filesystem, native
// keystroke bridge, etc.), expose them here via contextBridge.exposeInMainWorld.

export {};
